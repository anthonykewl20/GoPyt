module orders_multi.window

fn inside(day_placed: i64, day: i64) -> bool
    ensures result == (day - day_placed >= 0 and day - day_placed <= 30)
{
    elapsed = day - day_placed
    return elapsed >= 0 and elapsed <= 30
}
